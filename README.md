# setup
```zsh
$ brew bundle

$ ./setup.sh

$ sudo vi /etc/shells # add /usr/local/bin/zsh
$ chsh -s /usr/local/bin/zsh

$ pyenv install --list
$ pyenv install 3.9.5
$ pyenv global 3.9.5

$ ssh-keygen -t rsa
$ ssh-add ~/.ssh/id_rsa
$ ssh-add -l
```
